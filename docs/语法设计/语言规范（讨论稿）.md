# MPL 语言规范（讨论稿）

> 状态：讨论中，尚未冻结。本文用于统一语法讨论；只有标为“已确认”的规则，才能作为后续编译器实现的输入约定。

## 1. 定位与原则

MPL（Mindustry Processor Language）是一门高级语言。它先被编译为支持游戏基础宏的宏中间语言，再由后者编译为 Mindustry Logic（以下简称 mlog）处理器指令。MPL 的目标是让玩家能以结构化的方式书写变量、函数和控制流，并以高级对象模型访问游戏能力。

- 输出必须能映射到游戏支持的 mlog 指令与运行时能力。
- 常用流程应比手写跳转更清晰；编译器负责生成标签、跳转和临时变量。
- MPL 源码不出现 @；游戏基础能力只在宏中间语言以 @ 宏暴露。
- 语法与语义分开演进。本文先固定表面语法，类型系统、标准库和内存模型仍可调整。

## 2. 文件、字符集与注释（已确认）

- 源文件扩展名为 .mpl，文本编码为 UTF-8。
- 项目与包的 `src` 目录都可同时包含 .mpl 与 .mil 源文件；.mil 的宏规则见《宏中间语言设计（讨论稿）》。二者均不能读写或声明 Memory，内存只在各自的 `.mplh` 中以预算形式声明。
- 标识符暂限 ASCII：以字母或下划线开头，后接字母、数字或下划线。
- 空白字符不影响语义；语句以分号 ; 结束。
- 单行注释为 // 至行尾，块注释为 /* 与 */ 之间的内容。

~~~mpl
var retryCount: Int = 0;
retryCount += 1;

/* 这段代码会被编译器忽略。 */
~~~

## 3. 词法元素（已确认）

### 3.1 关键字

| 类别 | 关键字 |
| --- | --- |
| 声明 | var、val、const、fun、class、new |
| 模块与访问控制 | import、export、with、public、private |
| 类型 | Int、Float、String、Bool |
| 控制流 | if、else、for、while、do、break、continue、return |
| 布尔字面量 | true、false |
| 空字面量 | null |

关键字不能用作标识符。

### 3.2 字面量与内建符号

~~~mpl
42          // 整数字面量
3.14        // 小数字面量
.5          // 小数字面量
"hello"     // 字符串字面量，支持反斜杠转义
true        // 布尔字面量
null        // 只能赋给可空对象引用
~~~

@ 不是 MPL 的词法元素。它属于宏中间语言，用于访问游戏基础能力；详见《宏中间语言设计（讨论稿）》。

MPL 提供 `Unit` 与 `Building` 两个对象查询命名空间。它们在宏中间语言中映射到目标游戏内容，但不占用目标程序变量；首批 API 是 `Unit.getAll类型()` 与 `Building.getAll类型()`，用于构造可迭代查询对象。

## 4. 声明与类型

### 4.1 变量与常量（讨论基线）

~~~mpl
var health: Int = 100;
val name: String = "core";
const val maxRetries: Int = 3;
~~~

| 形式 | 意图 | 尚待确认 |
| --- | --- | --- |
| var | 可重新赋值的变量 | 是否允许未初始化声明 |
| val | 初始化后不可重新赋值的运行时值 | 是否允许全局 val |
| const val | 编译期常量，初始化式须可在编译期求值 | 是否保留 const 与 val 两个词 |

类型标注可省略，编译器从初始化表达式推导类型：

~~~mpl
var counter = 0;
val enabled = true;
~~~

未提供类型也没有初始化式的声明目前不允许；若需要支持，应在内存模型与默认值规则确定后再加入。

### 4.2 内建类型（讨论基线）

| 类型 | 用途 |
| --- | --- |
| Int | 数组下标、位运算与整数计数；目标层以数值保存，但不暴露游戏的 `flag` |
| Float | 坐标、距离、生命值、速度、时间与一般小数运算；映射到目标层 double 数值 |
| String | 由字符串运行时管理的文本值 |
| Bool | true / false 条件值；目标层以数值表达，但不能当作 Int 或 Float 使用 |
| Void | 无返回值函数的返回类型；不能作为变量或字段类型 |

Int、Float、String、Bool、Void 是语言层内建类型。类型名必须使用大驼峰；游戏对象、内容类型和链接建筑等同样使用大驼峰类型名。

### 4.3 可空对象（已确认方向）

MPL 默认引用不可空。只有对象引用类型可在类型名后加 `?`，例如 `Unit?`、`Building?`；`Int?`、`Float?`、`Bool?` 不存在。`null` 只能赋给可空对象类型。

~~~mpl
val target: Unit? = Radar.findEnemy();
if (target != null) {
    target.move(40, 20);
}
~~~

`if (value != null)` 的真分支会将 `value` 流缩窄为非空类型。第一版不把 mlog 的对象/数字隐式转换泄漏为 MPL 语义；安全调用 `?.` 与 Elvis `?:` 在独立语法阶段再加入。来自 `.mplh` 的链接常量是非空声明，动态 radar、locate、sensor 等观察结果才可为 null。

### 4.4 数值运算（类型规则已确认，表示范围待冻结）

- 不含小数点的数值字面量是 Int；含小数点的字面量是 Float。
- Int 与 Float 在算术和比较中允许 Int 向 Float 的安全提升；Float 不会隐式转换为 Int。
- Int / Int 的结果是 Float；位运算和数组下标只接受 Int。
- 对象、String、Bool 与数值之间不存在隐式转换。转换必须通过显式标准库 API 完成。
- Bool 只由 Bool 表达式产生；`&&` 与 `||` 保证短路，不把目标 mlog 的数值真值规则暴露给源码。

~~~mpl
var count: Int = 3;
var distance: Float = count + 0.5;
var half: Float = count / 2;
var index: Int = Int.floor(distance);
~~~

目标层以 `double` 保存数值，并把 NaN/Infinity 转换成 null 对象；因此 Int 的精确位宽、溢出、移位与除零，以及 Float 的非有限结果，必须在实现前写成 MPL 自己的总定义。详见《运行时存储、函数与跨 Tick 语义（讨论稿）》；不能直接沿用 mlog 的隐式转换或静默 null。

### 4.5 聚合类型（讨论基线）

~~~mpl
var point: (Float, Float) = (12.0, 34.0);
var queue: Int[10] = [1, 2, 3];
var dynamic: String[] = ["a", "b"];
~~~

- (T1, T2, ...) 表示元组类型；元组值用相同的圆括号形式书写。
- T[N] 表示长度为 N 的数组类型；T[] 表示长度尚未在类型中指定的数组。
- 数组字面量以方括号书写，元素以逗号分隔。

数组的存储位置、越界行为、动态长度和元组拆箱的代码生成尚未确定；这些形式先作为语法预留，不能承诺已可编译到 mlog。

## 5. 函数与调用（讨论基线）

~~~mpl
fun clamp(value: Float, lower: Float, upper: Float): Float {
    if (value < lower) {
        return lower;
    }
    if (value > upper) {
        return upper;
    }
    return value;
}

var result = clamp(8, 0, 5);
~~~

- 函数由 fun 声明，参数必须带类型标注。
- 返回类型位于右括号后的 : 之后；省略时表示无返回值（暂定）。
- return; 可用于无返回值函数；return expression; 用于有返回值函数。
- 第一版函数采用静态、不可重入 ABI，调用图不得有环；直接和间接递归均为编译错误。
- 实参从左到右求值；函数参数、局部变量、返回值和返回地址由编译器分配内部槽，`return` 通过内部 `@counter` 跳转完成。
- 函数可读取在其声明前已声明的顶层变量；编译器拒绝任何可能在该变量初始化前读取它的顶层调用路径。

游戏功能的高级 API 会以普通 MPL 调用形式提供；其具体集合尚待设计。它们在第一编译阶段被降级为宏中间语言的 @ 宏，而不是直接出现在 MPL 源码中。

## 6. 语句与控制流（已确认语法，语义待实现）

~~~mpl
if (oreCount > 0) {
    mineOre(oreX, oreY);
} else if (enemyNear) {
    defend();
} else {
    waitForWork();
}

for (var i: Int = 0; i < 10; i += 1) {
    tick(i);
}

while (enabled) {
    tick();
}

do {
    retry();
} while (failed);
~~~

代码块使用 { ... }。break; 只能离开最近的循环，continue; 只能进入最近循环的下一次迭代；这两条约束将在语义分析阶段检查。

### 6.1 单位与建筑遍历（已确认表面语法）

~~~mpl
for (var unit : Unit.getAllAlpha()) {
    unit.move(100, 80);

    if (unit.health < 30) {
        unit.stop();
    }
}

for (var turret : Building.getAllDuo()) {
    turret.shoot(targetX, targetY, true);
}
~~~

- `Unit.getAllAlpha()` 返回惰性、不可变 `UnitSet<Alpha>`；Alpha 是目标 profile 中的单位类型。`Building.getAllDuo()` 同理返回 `LinkedBuildingSet<Duo>`，它只遍历 `.mplh` 已声明且类型为 Duo 的链接建筑，绝不代表全地图同类建筑。
- 当控制循环只需要有限编队时，可在全部 `.where(...)` 之后调用 `UnitSet.take(n)`；它稳定私有认领至多 n 个单位，而不是按本轮 `ubind` 顺序临时截断。`flag`、owner token 和认领过程仍完全属于编译器 runtime。
- `set.size` 返回当前扫描结果的 `Int`；`set.get(index)` 返回可长期操作的 `Unit<Alpha>?`，越界为 null。它们都不是数组快照：每次读取可观察到游戏世界的新变化，index 也没有稳定顺序语义。
- for 中的 unit 是当前轮的 `Unit<Alpha>`。普通循环内可直接调用其方法；被保存到循环外时，编译器自动提升为长期句柄并计入隐藏资源预算。由 `get(...)` 得到的非 null Unit 可保存或作为函数返回值；单位死亡后 `unit.alive` 为 false。
- unit 的控制方法直接作用于当前遍历到的单位；建筑控制只对 profile 明确支持的链接建筑能力开放。游戏在该时刻拒绝控制时按目标层行为不生效，不产生 MPL 运行时异常。循环内的 break 与 continue 保持普通循环语义。
- UnitSet/LinkedBuildingSet 可保存为 `val` 并重复用于 `for`，但它们不是数组、不能构造快照、存入字段或跨包导出。
- 未找到可遍历对象时，循环体执行 0 次；遍历顺序和遍历期间新生成/销毁对象的处理不作保证。

UnitSet 是弱一致遍历：第一版禁止任何 Unit 迭代嵌套，且控制单位可能被其它处理器抢占。用户只使用 `Unit.getAll类型()`、`.where(...)`、`.take(n)`、`size`、`get(...)` 与 Unit 对象；不存在公开的 UnitManager、UnitPool、slot、flag、`manage()` 或 `service()` API。编译器可在底层以 `ubind`、隐藏 UnitRef 与隐藏 flag 完成单位识别和恢复，但这些实现细节不改变源语言。详见《单位遍历与私有运行时（讨论稿）》。

### 6.2 遍历过滤 lambda（已确认适用范围）

~~~mpl
for (var unit : Unit.getAllAlpha(unit => unit.health < healthLimit)) {
    unit.move(targetX, targetY);
}

for (var unit : Unit.getAllAlpha(_.health < healthLimit)) {
    unit.move(targetX, targetY);
}
~~~

以下写法等价，并允许先保存查询：

~~~mpl
val damaged = Unit.getAllAlpha().where(unit => unit.health < healthLimit);
for (var unit : damaged) {
    unit.move(targetX, targetY);
}
~~~

`.where(...)` 可连续调用；每个谓词都在前一个 UnitSet 上继续筛选，等价于按书写顺序短路的逻辑 AND：

~~~mpl
val repairable = Unit.getAllAlpha()
    .where(_.health < 50.0)
    .where(_.ammo > 0.0);
~~~

`_` 是单参数 lambda 的简写。第一版 lambda 只可作为 `getAll类型(...)` 或 `.where(...)` 的过滤谓词：它在遍历时内联执行，不可赋给变量、不可返回、不可保存，也不构成通用高阶函数系统。lambda 只可只读捕获外层标量，且不能控制单位、进行 I/O、赋值、`new` 或开启嵌套遍历。

## 7. 表达式与优先级（讨论基线）

从高到低的优先级如下。同一行除特别说明外按左结合处理。

| 优先级 | 形式 |
| --- | --- |
| 最高 | 分组 (expr)、调用 f(...)、下标 a[i]、属性 a.name、方法调用 a.name(...) |
| 一元 | !、~、+、-、前置 ++、前置 -- |
| 后缀 | 后置 ++、后置 -- |
| 乘法 | *、/、% |
| 加法 | +、- |
| 移位 | <<、>> |
| 比较 | <、>、<=、>= |
| 相等 | ==、!= |
| 位运算 | &、^、| |
| 逻辑 | &&、|| |
| 条件 | condition ? whenTrue : whenFalse |
| 最低 | =、+=、-=、*=、/=、%=、&=、|=、^=、<<=、>>= |

赋值与条件表达式按右结合处理。对象身份比较、通用属性/方法调用来自 v2 demo，尚未成为第一版承诺；Unit.getAll类型()、Building.getAll类型() 与 profile 明确提供的对象 API 是已确认的例外。

## 8. 暂定 EBNF

下列 EBNF 描述讨论基线，省略空白和注释规则：

~~~ebnf
程序          = { 导入声明 | 顶层声明 | 语句 }, EOF ;
导入声明      = "import", "{", 标识符, { ",", 标识符 }, "}", "from", 字符串,
                [ "with", "{", 资源实参, { ",", 资源实参 }, "}" ], ";" ;
资源实参      = 标识符, ":", 标识符 ;
顶层声明      = [ "export" ], ( 函数声明 | 类声明 | 常量声明 ) ;
函数声明      = "fun", 标识符, "(", [ 参数, { ",", 参数 } ], ")",
                [ ":", 类型 ], 代码块 ;
类声明        = "class", 标识符, "{", { 字段声明 | 方法声明 }, "}" ;
字段声明      = [ "public" | "private" ], 标识符, ":", 类型, ";" ;
方法声明      = [ "public" | "private" ], 函数声明 ;
常量声明      = "const", "val", 标识符, [ ":", 类型 ], "=", 表达式, ";" ;
参数          = 标识符, ":", 类型, [ "=", 表达式 ] ;
语句          = 表达式, ";"
              | ( "var" | "val" ), 模式, [ ":", 类型 ], "=", 表达式, ";"
              | 常量声明
              | if语句 | for语句 | 单位遍历语句 | while语句 | doWhile语句
              | "break", ";" | "continue", ";" | "return", [ 表达式 ], ";"
              | 代码块 ;
代码块        = "{", { 语句 }, "}" ;
模式          = 标识符 | "(", 标识符, ",", 标识符, { ",", 标识符 }, ")" ;
类型          = 非空类型, [ "?" ] ;
非空类型      = 基础类型 | 命名类型 | "(", 类型, ",", 类型, { ",", 类型 }, ")" | 类型, "[", [ 整数 ], "]" ;
基础类型      = "Int" | "Float" | "String" | "Bool" | "Void" ;
命名类型      = 标识符 ;
if语句        = "if", "(", 表达式, ")", 代码块,
                { "else", "if", "(", 表达式, ")", 代码块 }, [ "else", 代码块 ] ;
for语句       = "for", "(", ( 声明语句 | 表达式语句 | ";" ),
                [ 表达式 ], ";", [ 表达式 ], ")", 代码块 ;
单位遍历语句  = "for", "(", "var", 标识符, ":", 对象遍历, ")", 代码块 ;
对象遍历      = 对象查询 | 标识符 ;
对象查询      = 单位查询 | 建筑查询 ;
单位查询      = "Unit", ".", "getAll", 类型名, "(", [ 过滤lambda ], ")", { ".where", "(", 过滤lambda, ")" }, [ ".take", "(", 正Int字面量, ")" ] ;
建筑查询      = "Building", ".", "getAll", 类型名, "(", [ 过滤lambda ], ")", { ".where", "(", 过滤lambda, ")" } ;
过滤lambda    = 标识符, "=>", 表达式 | "_", 表达式 ;
类型名        = 标识符 ;
while语句     = "while", "(", 表达式, ")", 代码块 ;
doWhile语句   = "do", 代码块, "while", "(", 表达式, ")", ";" ;
~~~

表达式的完整产生式以第 7 节的优先级表为准；其中 `new 类型名(实参)` 为对象分配表达式。正式语法冻结时会补充 ANTLR 文法并以其作为可执行规范。

## 9. 与 mlog 的映射边界

MPL 不是独立虚拟机。变量赋值、算术、条件分支与高级运行时会先降级为宏中间语言；宏展开后才生成 set、op、jump 等 mlog 指令。函数调用、数组、元组、单位 runtime 与字符串表可能需要展开成标签、临时变量或内存单元。

因此，以下事项必须在实现前确定：

1. 变量、数组和递归调用如何在处理器有限的变量与内存中布局；
2. 函数参数、返回值和嵌套调用如何保存；
3. String 的字符串表、拼接与内存布局；
4. 宏中间语言的宏表如何跟随 Mindustry 版本更新；
5. 优化器能否在不改变游戏行为的前提下折叠常量和删除不可达代码。

## 10. 讨论记录

| 议题 | 当前建议 | 状态 |
| --- | --- | --- |
| 编译器实现语言 | Java（JDK 17） | 已确认 |
| 解析器 | ANTLR 4 | 倾向采用，待确认 |
| 基础类型 | Int、Float、String、Bool | 已确认 |
| 类型标注 | 声明可选、函数参数必填 | 讨论基线 |
| 数组与元组 | 保留 v2 表面语法 | 待内存模型确认 |
| 宏边界 | MPL 无 @；宏中间语言使用 @ 访问基础游戏能力 | 已确认 |
| 字符串表 | 字符输出块全局去重 | 已确认实现原则 |
| 对象遍历 | for (var unit : Unit.getAllAlpha()) / Building.getAll类型() | 已确认表面语法 |
| 遍历过滤 | 类型遍历 API 的单参数 lambda 与 _ 简写 | 已确认适用范围 |
| 单位运行时 | UnitSet 支持 `.where(...)`、`.take(n)`、`size`、`get(...)`；Unit 可由 get/提升获得长期句柄；flag/UnitRef/ID 均为私有 runtime | 公开 API 已冻结；v146 bind/flag lowering 待游戏内原型验证 |
| 模块系统 | import/export 与 with 非内存硬件注入；包端以 .mplh 声明资源需求 | 方向已确认，清单格式待定 |
| 对象模型 | class、public/private、this、new；无继承和自动 GC | 已确认方向，释放语义待定 |

变更本文时，请同时更新本表，并为会破坏现有示例的改动说明迁移方式。

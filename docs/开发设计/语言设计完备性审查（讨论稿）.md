# 语言设计完备性审查（讨论稿）

> 状态：v146 首个可运行闭环前的设计审查。本文不改变既有方向，只区分已冻结约束、可实现但尚未写全的细节，以及会阻塞后续实现的决议。

## 结论

MPL 的总体方向已经清晰：严格类型、高层 MPL 与可写宏的 MIL 分层、`.mplh` 硬件声明、编译器统一内存管理、无隐式主循环、v146 基线与 v159.7 可选优化。这足以继续实现基础控制流与函数。

但语言规范还**不完备**，不能在此阶段冻结为 1.0。以下 P0 项会决定生成代码的可观察行为，必须先写成确定规则；P1/P2 项可在对应功能开始实现前再冻结。

## 已明确且可作为实现输入的规则

- MPL 源码不能使用 `@`；MIL 才能使用受 profile 限制的基础宏。
- `.mplh` 只允许硬件与资源声明；MPL/MIL 不能直接访问 Memory。
- `Int`、`Float`、`Bool`、`String` 严格区分；仅允许 `Int → Float` 隐式提升，`Int / Int` 得到 `Float`。
- 顶层 `main.mpl` 按指令顺序执行，不生成虚假的 `while(true)`。
- 函数第一版使用静态、不可重入 ABI；禁止直接/间接递归。
- Unit/Building 使用编译器私有 runtime；用户看不到 flag 管理字段。
- `Message.print(...)` 自动 flush；`.mplh` 通过 `link("message1")` 显式绑定游戏 mlog 链接变量。
- 每次构建选择精确 target profile；v146 是基线，v159.7 只能做等价优化。

## P0：控制流、状态与数值语义

1. **作用域和生命周期**：是否允许顶层 `var` 被函数读取、局部变量遮蔽、顶层初始化每轮重执行的预期，以及未来 `state` 的精确引入方式。
2. **跨 tick 预算**：长循环、Unit 遍历、字符串输出被 IPT 切断时的可观察状态；哪些 runtime 操作必须可续执行，哪些只允许单 tick 完成。
3. **Int 定义**：mlog 实际以 double 存数值，需冻结 Int 的有效范围、溢出、负数取模、位运算截断、NaN/Infinity 与比较规则。
4. **条件真值**：Bool 只能来自 Bool 表达式；需要规定 mlog 数值转 Bool 的内部 lowering，以及短路 `&&` / `||` 是否保证不求值右侧。
5. **错误模型**：只做编译期失败已确认；尚需确定除零、无链接、对象为空、游戏权限拒绝等目标层情况是静态拒绝、生成默认值还是列为部署前提。

## P0：硬件绑定与 I/O

- 硬件通过游戏提供的 mlog 链接变量名精确映射，`.mplh` 不使用链接序号；仍需硬件检查命令验证名称、目标类型、链接是否存在及可写硬件冲突。
- `.mplh` 必须定义 link 类型、重复硬件、失效链接、可写硬件独占/共享，以及 package 注入冲突时的诊断。
- Display API 需冻结 Color、坐标、清屏、填充/描边状态、组合屏裁剪和 `flush()` 的缓冲区预算语义。
- Message API 需冻结字符串编码、400 字符上限、非字符串打印格式、自动 flush 与手动批处理的关系。
- 需为 v146 的 Read/Write/Control/Radar/Sensor/Unit Control 等指令建立机器可读签名，而非只在文档中列出名称。

## P0：函数、对象和内存

- `new` 的对象布局、构造函数语法、字段默认值、引用/值复制、`delete` 是否存在、对象身份比较与可空对象必须一并定义。
- 静态 ABI 已确定方向，但仍需定义参数求值顺序、返回值为 Void 的规则、全局初始化调用限制、临时变量预算和函数调用图的诊断位置。
- 物理/虚拟内存预算需要给出对象、数组、字符串表、函数槽位和 runtime 的统一计量单位及构建报告格式。
- 数组/元组的长度、初始化、边界检查策略和持久化布局尚未决议，因此不能进入稳定语法。

## P1：模块、MIL 和版本化

- 包解析需要冻结包名、registry/工作区规则、锁文件、版本选择、循环依赖、target 兼容性与包 `.mplh` 资源汇总格式。
- `export`、访问控制、类成员可见性、跨包类型名冲突与 re-export 尚无完整规则。
- MIL 需要独立 grammar、允许宏列表、宏卫生/命名冲突、源映射和 profile 禁止宏的诊断规则。
- profile 数据需要包含指令、内容、LAccess、权限、IPT、I/O 容量和地图规则前提；v159.7 每一项优化要有与 v146 等价的测试。

## P1：Unit / Building runtime

- 遍历终止条件、生成/销毁单位期间的行为、过滤 lambda 的捕获和求值次数、嵌套遍历限制、break/continue lowering 都需写成算法。
- 私有 flag 的范围、回收、与其他处理器冲突、玩家手写 mlog 干扰和无法控制单位时的处理需要部署契约。
- Building 的枚举范围、链接变化、控制权限和与 Display/Message 的统一硬件模型仍需补充。

## P2：开发者体验与发布契约

- 诊断编号表、错误恢复、来源区间、mlog 源映射、格式化器和 JSON 构建报告。
- 项目配置 schema、`init` 模板版本、`check`/`build`/硬件检查命令、输出覆盖策略。
- 标准库版本、弃用策略、兼容性保证、示例与游戏内 golden test 的版本矩阵。

## 建议的冻结顺序

1. 完成 P0 的控制流、Int、硬件唯一链接与 Message 细则；
2. 实现 if/while/for 与函数，使用 v146 golden mlog 测试验证 ABI；
3. 冻结对象/内存布局后再实现 `new`、数组和 String runtime；
4. 最后引入包、MIL 用户入口、Unit/Building runtime 与 v159.7 优化。

在每一项进入实现前，应把对应章节从“讨论稿”改为“已确认”，并补充可执行的正向、反向与 profile 测试。

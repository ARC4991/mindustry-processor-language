// @mpl/status-panel 的 hardware.mplh。
// 包只能在 .mplh 中声明需求，不能看到调用方的 link 编号或内存槽位。

require memory virtual(
    minSize: 512,
    mode: Pool
);

require screen: Display(
    minWidth: 176,
    minHeight: 176
);
